import ReactDOM from "react-dom";
import React from "react";

class TaxaApp extends React.Component {
  render() {
    return (
      <div>

      </div>
    );
  }
}

const domContainer = document.getElementById("react-taxa-app");
ReactDOM.render(<TaxaApp />, domContainer);